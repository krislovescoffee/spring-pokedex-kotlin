CREATE TABLE cash_card
(
    ID     BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NAME STRING NOT NULL DEFAULT ""
    HEIGHT_IN_CM NUMBER NOT NULL DEFAULT 0
    WEIGHT_IN_KG DOUBLE NOT NULL DEFAULT 0
);
